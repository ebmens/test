name: shell commands

on: [push]

jobs: 
   run shell commands:
     runs-on: ubuntu-18.04
     steps: 
        - name: checkout
          uses: actions/checkout@v3
        - name: echo string 
          run: echo "hello world"
        - name: multi string 
          run:
            node -v 
            npm -v